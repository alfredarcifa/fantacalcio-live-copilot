export function Progress({ value = 0, className = "" }) { return <div className={`overflow-hidden rounded-full bg-slate-800 ${className}`}><div className="h-full bg-emerald-500" style={{ width: `${Math.max(0, Math.min(100, value))}%` }} /></div>; }